# 主力 / 次主力换月规则

实现位置：`exchange_downloaders.ipynb` 第 14 节，函数 `tag_dominant()`。
产出：`dominant/main.parquet`、`dominant/sub.parquet`。

---

## 一、规则出处

采用米筐 rule0 的官方定义：

> 当同品种其他合约的持仓量在**收盘后**超过当前主力合约 **1.1 倍**时，
> 从**第二个交易日**开始切换主力合约。每个合约只能做一次主力/次主力，不会重复出现。

在此之上加了一条我们自己的**流动性门槛 `min_oi = 50`**（rule0 没有这道防护）。

---

## 二、状态变量

每个品种维护四个状态，跨年连续，不重置：

| 变量 | 含义 |
|---|---|
| `main` | 当前主力合约 |
| `main_k` | 主力的交割月水位线 |
| `sub` | 当前次主力合约 |
| `sub_k` | 次主力的交割月水位线 |

### 交割月怎么转成整数

`key = 年 × 12 + 月`

| 合约 | 交割月 | key |
|---|---|---|
| rb2510 | 2025-10 | 2025×12+10 = 24310 |
| rb2512 | 2025-12 | 2025×12+12 = 24312 |
| rb2601 | 2026-01 | 2026×12+1 = 24313 |

**必须转成整数再比**。直接比字符串会错：`"2601" < "2512"`，但 2026-01 明明比 2025-12 晚。

**郑商所的 3 位代码要先补出十位年份**。`SR701` 可能是 2017-01 也可能是 2027-01，
光看代码分不出来，得结合当前交易日推断（取 ≥ 当前年份的最近一个）。
不处理这一步会跨十年撞名，是踩过的坑。

---

## 三、每个交易日五步

### ① 流动性门槛

当日该品种**所有合约**的持仓量都 `< min_oi`（默认 50 手）
→ 不标注主力/次主力，**且状态原地冻结**（`main_k` / `sub_k` 不推进）。

冻结这一点是关键。僵尸品种全场零持仓时，按持仓量排序的结果是**随机的**；
不冻结的话，这个随机结果会被水位线永久固化。
实测 BB 胶合板 2024 年 5 月因此连错 164 天。

### ② 兑现昨天定下的切换

昨天收盘挂了待切换合约、且它今天在挂牌 →
`main` 换成它，`main_k` 更新为它的交割月，同时 **`sub_k` 抬到 `main_k`**。

如果新主力正是昨天的次主力，则 `sub` 置空，进入第 ③ 步重选。

### ③ 补位（仅在主力摘牌或首日）

主力今天不在挂牌了 → 在**交割月 ≥ `main_k`** 的合约里选持仓量最大的。
次主力同理，另外排除主力本身，且交割月 ≥ `sub_k`。

### ④ 记录当日 `main` / `sub`

### ⑤ 收盘后判断明天换不换

扫描同时满足两个条件的合约：

- 交割月 ≥ `main_k`（不能往回走）
- 持仓量 **> 1.1 × 当前主力的持仓量**

有的话，取其中持仓量最大的挂为「明天生效」。
次主力同一套逻辑，用 `sub_k` 和次主力自己的持仓量比。

---

## 四、四条设计各自解决什么

**1.1 倍触发，不是「谁大跟谁」。**
换月拉锯期两个合约持仓咬得很近，谁大跟谁会来回抖，每抖一次复权序列就注入一次价差。
留 10% 的迟滞带，只有明显反超才动。

**延后一天生效。**
收盘后才知道持仓量，当天追认不现实；也符合实盘移仓的节奏。

**交割月单调（水位线只升不降）。**
这是「每个合约只做一次」的实现方式。月份只增不减 ⇒ 一个合约天然只会连续担任一段主力。

⚠️ 不要用「记住所有当过主力的合约代码、永不复用」来实现这条。看着等价，实际会烂掉：
一次误切就把一个合约**永久烧掉**，真正的主力后面持仓再大也当不上。
实测 2024-01-02 白糖主力挂在 SR403（持仓 241,400），而真正的主力 SR405 有 449,414；
PTA 是 TA402（324,178）vs TA405（1,520,081），差 4.7 倍。
而且这种烂法**随时间累积**，2026 年主力准确率会跌到 83%。
水位线没这个问题——真正的主力只要月份不比当前旧，随时能接管。

**`sub_k` 在主力换月时抬到 `main_k`。**
不抬的话，刚卸任的主力会立刻变成次主力（它减仓中但往往还是全场第二）。
抬了之后它自动出局，次主力只会是接棒上来的更新合约。

---

## 五、次主力会不会跟着主力强制换月

**不会强制。次主力只在「它自己不成立了」的时候才换。**

全历史 4,209 次主力换月，分两种：

| 情况 | 占比 | 次主力是否跟着换 |
|---|---|---|
| **正常接棒**：新主力就是昨天的次主力 | 87.3% | **100% 换**（位置空出来了，必须重选） |
| **跳跃换月**：新主力是别的合约 | 12.7% | 只有 7.5% 换（原次主力退场了才换） |
| 合计 | | 88.2% |

跳跃换月的例子：

```
2025-04-16  豆一 a
  换月前：main = a2505   sub = a2511
  换月后：main = a2507   sub = a2511   ← 次主力留任
```

豆一这种一年只有 1/5/9 月主力的品种最典型：主力从 a2505 挪到 a2507，
但真正的第二大持仓一直是 a2511，没理由换掉它。

---

## 六、已知偏差

**次主力交割月偶尔早于主力：930 行，占 0.46%，涉及 20 个品种。**

`sub_k` 抬高只约束**下一次挑选**，不会把已经在任的次主力赶下台。
所以跳跃换月之后可能出现：

```
2013-02-05  沪铝  main = al1305   sub = al1304
```

这种情况下 al1304 确实是当日第二大持仓，标成次主力没错，只是月份比主力近。

**保持现状是有意的**：强制次主力永远比主力新，代价是要把一个持仓明明第二大的合约
换成一个更小的，反而不如实际情况准确。

---

## 七、实测表现

全历史 2010–2026 标注（225,008 个日×品种，有主力 203,545 个），
一致率在 2024–2026 上跟米筐 rule0 映射表比：

| 实现 | 对 rule0 一致 | 主力=当日持仓最大 | 主力<最大的50% | 倒退交割月 |
|---|---|---|---|---|
| 最早：5日均持仓 + 交割月棘轮 | 97.92% | 94.73% | 0.69% | 0 |
| 记名单版（已废弃） | 88.52% | 89.91% | 5.32% | 145 |
| **交割月单调版（现在）** | **99.51%** | **95.65%** | **0.36%** | **0** |

逐年稳定，无累积衰减：

| 年份 | 主力=当日持仓最大 |
|---|---|
| 2010 | 95.3% |
| 2015 | 96.4% |
| 2020 | 95.6% |
| 2024 | 96.0% |
| 2026 | 94.9% |

一致性自检（全历史）：

| 检查 | 结果 |
|---|---|
| 主力倒退交割月 | 0 次 |
| 次主力倒退交割月 | 0 次 |
| 同日 `main == sub` | 0 行 |
| 次主力是曾经的主力 | 0 行 |

---

## 八、为什么换掉最早的「5 日均持仓」版

5 日均值在旧合约临近交割**快速减仓**时反应太慢。

粳米 rr 2026 年 8 月：

| 日期 | rr2609 持仓 | rr2610 持仓 | 触发线 1.1×主力 | 判断 |
|---|---|---|---|---|
| 08-17 | 63,184（主力） | 41,436 | 69,502 | 41,436 < 69,502，不换 |
| 08-18 | 24,114（主力） | 42,014 | 26,525 | **42,014 > 26,525 → 挂待切换** |
| 08-19 | 3,250 | 42,854 | — | **主力换成 rr2610** |

08-18 rr2609 一天掉了 62%（临近交割的正常清仓），当天收盘就触发，次日生效。
旧规则要等 5 日均值追上，还得再等两天，报告里就会显示一个只剩 3,250 手的「主力」。

---

## 九、关于米筐自己

- rule0 的映射表里 1,203 次换月中有 **13 次**切到更早交割月（1.08%），集中在 rs、si、wr；
  另有 **158 个合约「中断后又回来当主力」**。
  所以「每个合约只做一次」在米筐自己的数据里也不是严格成立的硬约束，
  更像是对正常情况的描述。
- **不要直接套米筐的 rule0 映射表**：它 2026 年只有 80 个品种，
  **完全没有中金所**（if / ic / im / ih / t / tf / tl / ts 全缺），
  也没有 lf / ppf / vf 月均价合约，反而多标 8 个全年零持仓的僵尸品种。
  我们自己算的覆盖 97 个品种。

---

## 十、参数

```python
tag_dominant(df, min_oi=50, ratio=1.1)
```

| 参数 | 默认 | 说明 |
|---|---|---|
| `min_oi` | 50 | 流动性门槛，设 0 关掉 |
| `ratio` | 1.1 | 换月触发倍数，设 1.0 退化成「谁大跟谁」 |

改动规则后必须重跑 `build_dominant()`（全量约 4 分钟），
并且**一定要用全历史验证**——这类带累积状态的规则，
短窗口测出来的数字和 16 年连续跑完全是两回事（三年窗口测 99.68%，实际生产 88.52%）。
